import 'package:flutter/material.dart';

class Tab1View extends StatelessWidget {
  const Tab1View({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tab 1 View', style: TextStyle(fontSize: 24)));
  }
}
