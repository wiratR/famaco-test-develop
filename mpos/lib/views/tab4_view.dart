import 'package:flutter/material.dart';

class Tab4View extends StatelessWidget {
  const Tab4View({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tab 4 View', style: TextStyle(fontSize: 24)));
  }
}
