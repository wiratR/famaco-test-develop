import 'package:flutter/material.dart';

class Tab3View extends StatelessWidget {
  const Tab3View({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Tab 3 View', style: TextStyle(fontSize: 24)));
  }
}
